# Customize DB Port to avoid colision with dev one
embedded-db:
  name: db
  schema: trade
  port: 5436
# Core spring settings
spring:
  profiles:
    active: test
  main:
    allow-bean-definition-overriding: true